/**
 * The diary's faces live in src/lib/fonts.ts, shared with the product shell.
 * This re-export keeps the diary components' import stable.
 */
export { fontVars as diaryFontVars } from '@/lib/fonts'
