/**
 * The pen marks live in src/components/brand/PenMarks.tsx, shared by the diary
 * and the product's diary moments. This re-export keeps the diary's imports stable.
 */
export * from '@/components/brand/PenMarks'
